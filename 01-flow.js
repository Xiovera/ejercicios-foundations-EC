/*
 *
 * Lección 04: JS Foundations
 * Invertir Arreglo
 *
 */

var invert = array => {
	 return array.reverse();
	//console.log (invert);	
};

console.log(invert(['fresa','banana','piña']));

module.exports = invert;
